<div class="container">
<%= simple_form_for @cocktail do |f| %>
  <%= f.input :name %>
  <%= f.submit class: 'btn btn-primary' %>
  <% end %>
  <div class="btn btn-success"><%= link_to "Cocktail List", cocktails_path%></div>
</div>
